{
  "installCommand": "npm install --legacy-peer-deps && npm install ajv ajv-keywords --legacy-peer-deps",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 12,22 * * *"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
